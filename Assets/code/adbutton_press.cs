/*
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class adbutton_press : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        private int dropIteration = 30;
        Vector3 tempPos = transform.position;
    }

    // Update is called once per frame
    void Update()
    {
        if candlesDropped == dropIteration
        {
            int spawnHeight = Random.Range(5,15);
            
            tempPos.y = spawnHeight;




        }
    }

    virtual protected void OnMouseDown(){

        dropIteration += 1;



    }
}

*/